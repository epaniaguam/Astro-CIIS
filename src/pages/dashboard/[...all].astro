---
import LayoutRegister from "../../layouts/LayoutRegister.astro"
import { CURRENT_CIIS_EDITION } from "../../config";
import App from "../../views/panel/App"

export function getStaticPaths() {
  return [
    { params: { all: "" } },
    { params: { all: "postmaster" } },
    { params: { all: "ciis" } },
    { params: { all: "workshops" } },
    { params: { all: "account" } },
    { params: { all: "attendance" } },
  ];
}
---
<LayoutRegister title=`Dashboard | Congreso Internacional de Informática y Sistemas ${CURRENT_CIIS_EDITION}`>
  <App client:only />
</LayoutRegister>